1. Створити клас, що розширює Thread
Створити клас  Thread, NewThread  що розширює  клас.
Перевизначити метод  run(). У циклі for вивести на консоль символ 100 разів.
Створити екземпляр класу та запустити новий потік. 

2. Створити клас, що реалізує Runnable
Створити клас, що реалізує інтерфейс Runnable.
Перевизначити  метод. Створити цикл. У циклі роздруковуємо значення від 0 до 100, що діляться на 10 без залишку. run()for
Використовуємо статичний метод Thread.sleep() щоб зробити паузу.
Створити три потоки , що виконують завдання друку значень.

3. Виведення символу у трьох потоках
Необхідно створити три потоки , які змінюють той самий обєкт. Кожен потік повинен вивести на екран одну літеру 100 разів, а потім збільшити значення символу на 1. 

Створити клас, що розширює Thread. 
Перевизначити метод run()- тут буде синхронізований блок коду. 
Для того щоб три обєкти потоку мали доступ до одного обєкта, створюємо конструктор, що приймає на вхід StringBuilder обєкт. 
Синхронізований блок коду отримуватиме монітор на обєкт StringBuilder із пункту 3. 
Усередині синхронізованого блоку коду виведіть StringBuilderна екран 100 разів, а потім збільште значення символу на 1. 
У методі main()створіть один обєкт класу StringBuilder, використовуючи символ 'a'.
Потім створіть три екземпляри обєкта нашого класу та запустіть потоки. 


4. Змінити клас MyQueue
Змінити клас MyQueueз Потокові комунікації :

Замість int додати Queue<T> ( MyQueue зробити узагальненим), яка міститиме обєкти створювані  Producer. 
Додайте ще один обєкт Consumer, який запускатиметься також окремим потоком. 
Виводьте на консоль який із обєктів Consumer обробив обєкт із черги. 
Змініть цикл for на безкінечний цикл.